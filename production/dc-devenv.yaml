version: "3.9"

services:
  test:
    image: mongo:latest
    networks:
      - devenv
    environment:
      MONGO_INITDB_ROOT_USERNAME: mongodb
      MONGO_INITDB_ROOT_PASSWORD: mongodb
    # ports:
    #   - 27017:27017
    volumes:
      - mongo_test:/data/db

    # deploy:
    #   placement:
    #     constraints:
    #       - node.hostname == co2

volumes:
  mongo_test:

networks:
  devenv:
    external: true
